<div class="event">

  <div class="top">
    <div class="left-center-column name-date">
      <h2 class="card-title"><%= title %></h2>
      <p><%= date %></p>
    </div>

    <div class="center-row tags">
      <p>tags: </p>
      <% for( let i = 0; i < tags.length; i++ ) { %>
        <div class="tag">
          <p><%= tags[i] %></p>
        </div>
      <% } %>
    </div>
  </div>

  <div class="bottom">

    <p class="div-paragraph"><%= description %></p>

    <a class="dark-button" href="events/<%= title %>">View</a>

  </div>

</div>